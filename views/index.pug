extends ./application.pug
block content
  h1 Blog posts
  ul
    each post in posts
      .card.blue-grey.darken-1
        li(class="card-title") Title:  #{post.title}
        p(class="card-text") Message: #{post.body}
